doctype html
html(lang="ko")
    head
        //- Global site tag (gtag.js) - Google Analytics
        script(async src="https://www.googletagmanager.com/gtag/js?id=G-9SVY2KLBTZ")
        script.
            window.dataLayer = window.dataLayer || [];
            function gtag(){dataLayer.push(arguments);}
            gtag('js', new Date());
            gtag('config', 'G-9SVY2KLBTZ');

        //- Meta
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0")
        meta(name="theme-color" content="#2e394d")
        meta(name="author" content="솔톤세무회계")
        meta(name="title" content="솔톤 VIP 재무리포트")
        meta(name="description" content=`${metaDescription}`)
        meta(name="keywords" content="VIP, 재무리포트, 재무관리, 사장님, CEO, 가계부, 손익계산서, 현금흐름표, 인건비 관리, 급여 대장, 세무사, 세금 컨설팅, 재테크")

        //- Robots
        meta(name="robots" content="index, follow")

        //- Search
        link(rel="canonical" href=`https://soltonetax.com${canonicalUrl}`)

        //- Open Graph protocol
        meta(property="og:title" content="솔톤 VIP 재무리포트")
        meta(property="og:type" content="website")
        meta(property="og:image" content=`${ogImageUrl}`)
        meta(property="og:image:alt" content="솔톤 VIP 재무리포트")
        meta(property="og:description" content=`${metaDescription}`)
        meta(property="og:url" content=`https://soltonetax.com${canonicalUrl}`)
        meta(property="og:locale" content="ko_KR")
        meta(property="og:site_name" content="솔톤 VIP 재무리포트")

        //- Styles
        link(rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Nunito&display=swap")
        link(rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Nunito&display=swap")
        link(rel="stylesheet" type="text/css" href="/static/styles.css")

        //- Favicon & Title
        link(rel="shortcut icon" href="https://soltonetax.com/favicon.ico")
        title 솔톤 VIP 재무리포트
        
    body
        include ../partials/headerVip
        main
            block content
        include ../partials/footer

        button.topBtn(title= "Go to top") Top
        
        script(src="/static/main.js")


        //- Naver Analytics
        script(type="text/javascript" src="//wcs.naver.net/wcslog.js")
        script.
            if(!wcs_add) var wcs_add = {};
            wcs_add["wa"] = "9eaa3501e4e0d0";
            if(window.wcs) {
                wcs_do();
            }
