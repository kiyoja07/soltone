// Global

const HOME = "/";
const PEOPLE = "/people";
const SITEMAP = "/sitemap.xml";

// Admin

const ADMIN = "/admin";
const REQUEST_BLOG = "/request-blog";
const GET_BLOG = "/get-blog";

// Routes

const routes = {
  home: HOME,
  people: PEOPLE,
  sitemap: SITEMAP,

  admin: ADMIN,
  requestBlog: REQUEST_BLOG,
  getBlog: GET_BLOG,
};

export default routes;
